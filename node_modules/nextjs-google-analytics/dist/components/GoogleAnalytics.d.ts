/// <reference types="react" />
import { ScriptProps } from "next/script";
declare type GoogleAnalyticsProps = {
    gaMeasurementId?: string;
    gtagUrl?: string;
    strategy?: ScriptProps["strategy"];
    debugMode?: boolean;
};
declare type WithPageView = GoogleAnalyticsProps & {
    trackPageViews?: boolean;
};
declare type WithIgnoreHashChange = GoogleAnalyticsProps & {
    trackPageViews?: {
        ignoreHashChange: boolean;
    };
};
export declare function GoogleAnalytics({ debugMode, gaMeasurementId, gtagUrl, strategy, trackPageViews, }: WithPageView | WithIgnoreHashChange): JSX.Element | null;
export {};
